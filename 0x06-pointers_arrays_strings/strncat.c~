#include "main.h"

/**
 *
 *
 */

char *_strncat(char *dest, char *src, int n)
{
	char cat_n = dest;

	while (dest != '\0')
	{
		dest++;
	}
	while (src != '\0')
	{
		*dest = *src;
		dest++;
		src++;
		n--;
	}
	dest = '\0';
	return (cat_n);
}
