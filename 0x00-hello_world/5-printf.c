#include <stdio.h>

/**
 * main - Prints text exactly followed by a new line
 * Return: 0 after program executes
 *
*/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
